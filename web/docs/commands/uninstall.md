# uninstall

The `uninstall` command allows you to remove applications from your device directly from the terminal.

## Syntax
```
uninstall <app name>
```

## Usage
- `uninstall <app_name>`: Requests to uninstall the specified application. A system confirmation dialog will appear to confirm the removal.

## Example
```
uninstall Instagram
```
