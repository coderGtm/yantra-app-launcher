# time

The `time` command displays the current date and time.

## Syntax
```
time [utc] [GMT]
```

## Usage
- `time`: Shows the current local date and time.
- `time utc`: Shows the current UTC time.
- `time utc +5:30`: Shows UTC time with a specified offset.

## Example
```
time
time utc +5:30
```
