# cmdrequest

The `cmdrequest` command allows you to request new features or commands for Yantra Launcher.

## Syntax
```
cmdrequest
```

## Usage
Executing `cmdrequest` will open a GitHub issue template specifically for command requests. While it doesn't guarantee the addition of the command, it's the best way to let the developer know what features you'd like to see in future versions.
