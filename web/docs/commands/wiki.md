# wiki

The `wiki` command opens the Yantra Launcher wiki in your default web browser.

## Usage

```text
wiki
```

The command opens:

<https://codergtm.github.io/yantra-app-launcher/docs/>

!!! note
    A web browser that handles URL requests must be installed on your device.
