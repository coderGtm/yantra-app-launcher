# settings

The `settings` command opens the main configuration panel for Yantra Launcher.

## Syntax
```
settings
```

## Usage
Executing `settings` will launch a GUI where you can customize various aspects of the launcher, including configurations, behaviors, AI setup, and more.
