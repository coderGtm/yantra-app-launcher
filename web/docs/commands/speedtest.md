# speedtest

The `speedtest` command opens a utility to check your current internet connection speed.

## Syntax
```
speedtest
```

## Usage
Executing the `speedtest` command will launch a small GUI utility powered by [OpenSpeedTest.com](https://openspeedtest.com) within the launcher to measure your download and upload speeds.
