# clear

The `clear` command is used to clear the terminal screen.

## Syntax
```
clear
```

## Usage
Executing `clear` will remove all previous command outputs and text from the terminal, giving you a fresh, empty workspace.
