# notify

The `notify` command allows you to trigger a system notification with a custom message.

## Syntax
```
notify <message>
```

## Usage
- `notify <message>`: Fires a notification displaying the provided text.

## Example
```
notify Finished setting up the workspace
```
